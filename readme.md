# Simple web site using Django ORM

Simple written in Python using Django and  SQLite3 database

# Requirements:
```
Python>=3.6.1
Packages:
    Django
Database:
    SQLite
```


# Run site
```
python manage.py runserver
```